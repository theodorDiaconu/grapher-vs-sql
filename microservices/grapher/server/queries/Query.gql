type Query {
  users: [User]
  posts(_id: String, first: Int): [Post]
  tags: [Tag]
  comments: [Comment]
  groups: [Group]
  postCategories: [PostCategory]
}

type Mutation {
  addPost(title: String): ID
}
